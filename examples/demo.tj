# TJLang Demo File
# This file contains various TJLang constructs for testing

# Type mismatch error
x: int = "hello"

# Undefined variable error
print(unknown_var)

# Non-exhaustive match error
option: Option<int> = Some(42)
match option {
    Some(x) => x
}

# Parser error - missing parameter name
def func( -> int {
    return 42
}

# Unused variable warning
unused_var: int = 42

# Unterminated string error
str = "unterminated string
